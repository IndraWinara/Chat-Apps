/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {
    domains : ['creazilla-store.fra1.digitaloceanspaces.com','i0.wp.com']
  }
}

module.exports = nextConfig
